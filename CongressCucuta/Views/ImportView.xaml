<UserControl x:Class="CongressCucuta.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CongressCucuta.Views"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="640">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType = "Button">
            <Setter Property = "FontFamily" Value = "Candara"/>
            <Setter Property = "FontSize" Value = "16"/>
            <Setter Property = "Padding" Value = "12, 6, 12, 6"/>
        </Style>
        <Style TargetType = "TextBlock">
            <Setter Property = "FontFamily" Value = "Nirmala UI"/>
            <Setter Property = "FontSize" Value = "16"/>
        </Style>
    </UserControl.Resources>
    
    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height = "*"/>
            <RowDefinition Height = "Auto"/>
            <RowDefinition Height = "*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row = "1" Command = "{Binding ChooseSimulationCommand}" HorizontalAlignment = "Center">
            Choose a simulation
        </Button>
        <TextBlock Grid.Row = "2"
                   Visibility = "{Binding WasChoiceFailure, Converter = {StaticResource BooleanToVisibilityConverter}}"
                   HorizontalAlignment = "Center"
                   Margin = "6">
            Failed to open file
        </TextBlock>
    </Grid>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height = "5*"/>
            <RowDefinition Height = "1*"/>
            <RowDefinition Height = "5*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width = "1*"/>
            <ColumnDefinition Width = "2*"/>
            <ColumnDefinition Width = "2*"/>
            <ColumnDefinition Width = "1*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row = "0"
                   Grid.Column = "1"
                   HorizontalAlignment = "Center"
                   VerticalAlignment = "Bottom"
                   Margin = "0, 0, 0, 6">
            Select simulation
        </TextBlock>
        <ComboBox Grid.Row = "1"
              Grid.Column = "1"
              ItemsSource = "{Binding Simulations}"
              SelectedIndex = "{Binding SelectedIdx}"
              HorizontalAlignment = "Center"
              VerticalAlignment = "Center"
              Padding = "6, 0, 0, 2">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock FontFamily = "Nirmala UI Semilight" FontSize = "16" Text = "{Binding Name}"/>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
        <Button Grid.Row = "2"
                Grid.Column = "1"
                Command = "{Binding ChooseSimulationCommand}"
                HorizontalAlignment = "Center"
                VerticalAlignment = "Top"
                Margin = "6">
            Open
        </Button>
        <TextBlock Grid.Row = "1"
                   Grid.Column = "1"
                   Grid.ColumnSpan = "2"
                   HorizontalAlignment = "Center"
                   VerticalAlignment = "Center"
                   Margin = "12">
           or
        </TextBlock>
        <Button Grid.Row = "1"
                Grid.Column = "2"
                Command = "{Binding ImportSimulationCommand}"
                HorizontalAlignment = "Center"
                VerticalAlignment = "Center">
            Import from file
        </Button>
        <TextBlock Grid.Row = "2"
                   Grid.Column = "2"
                   Foreground = "Red"
                   Visibility = "{Binding WasImportFailure, Converter = {StaticResource BooleanToVisibilityConverter}}"
                   HorizontalAlignment = "Center"
                   Margin = "6">
                   Failed to open file
        </TextBlock>
    </Grid>
</UserControl>
