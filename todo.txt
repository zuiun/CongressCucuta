make sure that all the views (slideview) have a viewmodel attached eventually. i just removed them to test how it works because idk how to make lineview work
bind title of simulationwindow to the country name
display textblock on filewindow if file fails
